# 개발 기록 로그 — iNote (FE)

> 다른 PC에서 작업을 이어받을 때 이 파일을 먼저 읽고 맥락을 파악합니다.
> 작업할 때마다 아래 로그에 날짜 + 내용을 추가합니다. BE/AI 쪽 상세 내용은 각 레포의
> `DEV_LOG.md`(`inote-server`, `inote-ai`) 참고 — 여기는 FE 관점 기록.

---

## 빠른 현황 파악

| 항목 | 상태 |
|------|------|
| 레포 위치 | https://github.com/seo337dc/inote |
| 실행 포트 (로컬) | `http://localhost:3011` |
| 배포 URL | https://inote-blog.vercel.app/ _(도메인 변경 예정)_ |
| 현재 진행 단계 | 글쓰기 + LLM 챗 어시스턴트(세션/검색) + AI 자동 요약 + 카테고리 관리까지 완료 (2026-09-11). 할일 리스트·일지·독서·대시보드는 페이지만 있고 "준비 중" |

---

## 작업 로그

### 2026-09-14 — 회원 탈퇴 UI + 세션 목록 이동 버튼 + 만다라트 갱신

- `/profile` 페이지 하단에 "위험 구역" 섹션 추가 — 확인 팝업(Dialog) → `DELETE /users/me`
  호출 → 로그아웃 → 홈 이동. `ProfileForm.tsx`의 나머지(닉네임/비밀번호 변경)는 아직 목업이라
  이번엔 손대지 않고 탈퇴 기능만 실제 API로 연동
- 세션 목록에서 글쓰기 세션 항목 오른쪽 끝에 "그 글로 이동"(↗) 버튼 추가 — 세션 선택과 별도
  링크라 클릭해도 세션 전환 로직은 안 건드림, `post_id` 없는 일반 세션에는 버튼 자체가 안 뜸
- `docs/mandalart.html`/`MandalartGrid.tsx` 완료 항목 갱신(t2 축 대부분, t4 Groq 채팅 연동,
  t7 Vercel+Render+Neon)

### 2026-09-11 — LLM 챗 어시스턴트 UI + 세션/검색 + 메뉴 정리 + 빌드 에러 수정

`inote-server`/`inote-ai`에 draft/발행 모델, AI 자동 요약, 대화 세션 저장·검색 API가 갖춰짐에
따라 FE도 이어서 구현. 백엔드 설계·엔드포인트는 `inote-ai/DEV_LOG.md` 참고, 여기는 FE 구현과
UI 의사결정 위주로 기록.

#### ✅ 챗 입력창 개선

- 인풋박스 높이 확대(`rows=3`, `min-h-16 max-h-40`)
- `Enter` = 즉시 전송, `Shift+Enter` = 줄바꿈 — `keydown` 핸들러에서 `preventDefault` 여부로 분기

#### ✅ 대화 세션 저장/전환

- `useChatMessages` 훅에 `sessionId`/`sessionPostId` 상태 도입 — `/write?id=X`처럼 글쓰기
  페이지에 있을 때는 그 글의 postId를 세션 id로 강제 전환(라우트가 바뀌면 재동기화), 그 외에는
  클라이언트가 만든 UUID로 일반 세션 시작
- 세션을 보낼 때마다(매 전송 시점) 그 글의 최신 제목/카테고리를 실시간으로 다시 가져옴 —
  세션 전환 시점에 한 번만 캐싱하면 자동저장으로 막 바뀐 제목을 못 따라가는 문제가 있었음
- "새 대화 시작"/"세션 목록에서 선택" 둘 다 지원, 목록은 `GET /sessions`로 로드하고 검색어
  입력 시 300ms 디바운스 후 `q` 파라미터로 재조회

#### ✅ 세션 목록 패널 — UI는 4번 방향을 바꿨다

한 번에 완성되지 않고 실제로 써보면서 계속 다듬음:

1. **왼쪽에 항상 떠 있는 사이드바** — 처음 요청대로 만들었지만 채팅 공간을 항상 잡아먹는 문제
2. **하단 아이콘으로 여닫는 토글 패널, 채팅 영역을 통째로 대체** — 클릭하면 채팅이 안 보이고
   목록만 보이는 방식. "이건 사이드 패널이 아니지 않냐"는 피드백으로 폐기
3. **채팅 옆에 나란히 두는 side-by-side** — 패널이 열리면 위젯 전체 폭이 늘어나서 채팅 폭은
   그대로 유지하는 방식
4. **최종: 채팅 위에 슬라이드로 겹쳐지는 오버레이** — 클로드 자체 UI의 사이드바 토글 패턴
   참고. 데스크탑은 채팅 영역의 80%만 덮고 나머지 20%는 `bg-black/20`으로 딤 처리(클릭하면
   패널 닫힘), 모바일은 시트 전체를 덮음 — 화면이 좁아서 20% 틈을 남기는 게 의미 없었음

`ChatSessionPanel`이 `<ChatBody/>`+`<ChatComposer/>`가 있던 자리에 조건부로 끼워지는 게
아니라, 독립된 `absolute` 오버레이로 존재해서 `translate-x` transition만으로 슬라이드 인/아웃.

#### ✅ 카테고리 영역 — 홈 → 나의 글로 이동

처음엔 홈 페이지 왼쪽에 카테고리 필터 사이드바를 뒀는데, 돌아보니 "내 글을 관리하는" 성격이
강해 홈에서는 빼고 "나의 글" 페이지로 옮김. `CategoryFilter`에 `basePath` prop을 추가해서
`/my-posts` 기준으로 필터 링크가 생성되도록 함. 모바일 하단 메뉴 드로어의 카테고리 필터는
홈/나의 글 양쪽에서 다 열리도록 유지(모바일은 사이드바가 없어서 드로어가 유일한 진입점이라).
사이드바 라벨 옆에 `/categories`(카테고리 관리 페이지)로 가는 "관리" 뱃지 버튼 추가.

#### ✅ 메뉴 정리

- 대시보드는 주석 처리로 보류(삭제 아님, 언제든 복구 가능)
- "할일 리스트" → "할 일"로 라벨 변경
- "카테고리" 메뉴 자체는 없애고, 카테고리 관리 페이지로 가는 경로를 홈/나의 글/글쓰기 각
  화면에 자연스럽게 심어둠

#### 🐛 발견한 버그 — 모바일 글 상세 페이지 가로 스크롤

`LLM을 헤더 옆에 두는 레이아웃`(`header|llm`, `main|llm`)으로 바꾸면서 생긴 사이드 이펙트.
처음엔 `.prose`의 줄바꿈 규칙(`overflow-wrap`) 문제로 보였지만, 그건 보조적인 원인이었고
진짜 원인은 그 레이아웃의 최상위 `flex` 컨테이너에 `min-w-0`이 빠져 있던 것 — flex item
기본값 `min-width: auto` 때문에 안쪽 콘텐츠가 뷰포트보다 넓게 밀려나는 전형적인 flex
오버플로우 버그였다. `min-w-0` 하나 추가로 해결.

#### 🐛 발견한 버그 — 프로덕션 빌드 실패 (`/categories`)

`pnpm build`에서 `useSearchParams() should be wrapped in a suspense boundary`로 실패.
`ChatWorkspace`(전역 레이아웃의 LLM 위젯)가 내부적으로 `useSearchParams`를 쓰는데, 서버
데이터 페칭이 없어 정적 페이지로 프리렌더되는 `/categories`에서 이 훅 때문에 걸림. 이미
`<Nav>`에 적용돼 있던 것과 같은 패턴으로 `<ChatWorkspace>` 전체를 `<Suspense>`로 감싸서 해결.

#### 🧹 정리

- 글쓰기 페이지 자동저장 전 상태에 남아있던 "LLM 챗으로 초안 작성 기능은 Phase 2(inote-ai
  연동)에서 추가 예정" 안내 문구 제거 — 이미 구현된 기능이라 더 이상 맞지 않는 문구였음

#### 검증

- 매 기능마다 실제 테스트 계정으로 가입 → 브라우저에서 동작 확인 → 테스트 계정/세션 데이터
  정리(inote-server DB + inote-ai DB 양쪽) 반복
- `tsc --noEmit`, `eslint`, `pnpm build` 모두 통과 확인 후 커밋
