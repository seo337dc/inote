# inote-blog — 기획서

> `STRATEGY.md`가 "무엇을 만들지"라면, 이 문서는 "왜 만들고 무엇을 성공으로 볼지"를 정의한다.
> devlog-llm의 PLANNING.md 형식과 원칙을 이어받되, iNote 시리즈 통합에 맞춰 다시 잡았다.

---

## 배경 — devlog-llm에서 여기로

원래 "devlog-llm"이라는 개인 전용 블로그+RAG 실험 프로젝트로 시작했으나, 큰 틀을 다시 잡으면서
iNote 시리즈(`inote-money`와 같은 계열)로 편입하기로 함. 가장 큰 변화는 인증·DB를 처음부터 새로
만들지 않고, 이미 존재하는 `inote-server`(iNote 공통 백엔드)의 Better Auth·Prisma·DB를 그대로
재사용한다는 것 — devlog-llm에서 겪었던 "로그인부터 다시 만들어야 하나" 고민 자체가 사라짐.

## 큰 틀

**노션 + 블로그 + LLM**

## 목표

1. **멀티유저** — 우선은 내 실제 이메일 계정 2개(Gmail, Naver 등)로 로그인해서, 계정별로 데이터가
   실제로 잘 분리되는지 확인하는 것부터. (완전한 다중 사용자 서비스로의 확장은 그 다음 단계)
2. **다양한 용도** — 학습 기록, 이직 준비, 일기, 블로그, 기록 등 여러 종류의 글쓰기를 하나의
   서비스에서 다루기
3. **LLM 기능** — 작성한 글을 LLM이 자동으로 분석·평가하고, 리서치(조사)를 보조하고, LLM과
   대화하면서 문서를 작성할 수 있게 하기
4. **학습** — 이 프로젝트를 진행하면서 AI/LLM과 백엔드(Python) 기술을 실전으로 익히는 것 자체도
   목표 중 하나

## 아키텍처 결정 (2026-09-01 확정)

| 컴포넌트 | 역할 | 레포 |
|---|---|---|
| `inote-blog` | 블로그/노션 스타일 UI (Next.js) | 신규 — 이 레포 |
| `inote-server`의 `blog` 모듈 | 글 CRUD API, 인증 | 기존 레포 확장 (Better Auth·Prisma·Neon DB 그대로 재사용) |
| 신규 Python/FastAPI 서비스 | LLM 채팅·임베딩·RAG | 신규 레포, **별도 DB** |

- Python 서비스는 자체 로그인 로직이 없음 — FE가 이미 인증된 `user_id`/이메일을 요청에 그대로
  실어 보내고, Python은 그 값을 신뢰해서 데이터에 태그만 함 (devlog-llm의 `session_id` 패턴과 동일).
  진짜 인증 검증이 필요해지면 그때 추가.
- Python이 RAG를 위해 블로그 글 내용이 필요할 땐, DB를 직접 조회하지 않고 **API로 가져옴**
  (Postgres는 DB가 다르면 조인이 안 되므로).
- devlog-llm에서 이미 검증한 것들(Groq SSE 스트리밍, `conversations` 저장 패턴, pgvector 임베딩
  설계 대안 A/B, Voyage AI 임베딩 모델 조사)은 그대로 재사용.

## 만다라트 — 핵심 목표 + 8대 축

**핵심**: 노션 + 블로그 + LLM

| # | 축 | 상태 |
|---|---|---|
| 2 | 노션 + 블로그 UI와 BE 기능 개발 | 확정 |
| 3 | BE 기술 학습 | 확정 |
| 4 | LLM + MCP 연동 | 확정 |
| 5 | AI 관련 기술 학습 | 확정 |
| 6 | 프로젝트/프로덕트 기획 | 확정 |
| 7 | Infra, DevOps 적용 (처음엔 무료 티어로 간략하게 → 이후 실제 AWS 적용) | 확정 |
| 8 | Playwright 브라우저 자동화 및 E2E 테스트 | 확정 |
| 9 | 미정 | 보류 |

7개 축의 실천 항목(56칸)까지 채운 전체 만다라트는 [`docs/mandalart.html`](./docs/mandalart.html)
(브라우저로 열어서 확인) 참고. 9번이 정해지면 마저 채울 것.

## 성공 기준 (devlog-llm과 동일한 관점 유지)

기능 완성이 아니라 아래를 관찰한다:

- 이번 주에 실제로 글을 썼는가 (기록 습관이 붙는가)
- LLM 분석·평가 결과가 "다시 봐도 쓸모 있다"고 느껴지는가
- 여러 계정(Gmail/Naver)으로 실제로 써보면서 데이터 분리·인증 흐름에 문제가 없는가
- 만든 기능 중 실제로 다시 쓴 것 vs 한 번도 안 쓴 것

## 원칙 (devlog-llm에서 계승)

1. 가설 → 최소 구현 → 직접 사용 → 기록 → 다음 가설. 이 순서를 건너뛰지 않는다.
2. 미리 설계하지 않는다. 필요해질 때, 필요한 만큼만 추가한다 (YAGNI).
3. 기획·UX·아키텍처는 AI가 임의로 결정하지 않는다 — 대안 제시 → 사람이 승인 후 반영.
4. AI(Claude)가 제안하는 기능 확장은 기본적으로 의심한다.
5. 매 단계마다 기록한다 — 무엇을 시도했고, 뭐가 맞았고 틀렸는지.

## 미결정 사항

- [ ] 만다라트 9번 축 (8번은 Playwright E2E 테스트로 확정)
- [ ] Python AI 서비스의 레포 이름
- [ ] 임베딩 모델 최종 확정 (Voyage AI `voyage-4-lite` 유력, devlog-llm에서 조사한 내용 재사용 가능)
- [ ] `inote-server`의 Neon DB를 그대로 확장할지, 별도 브랜치를 팔지
